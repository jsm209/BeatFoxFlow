using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spikeTrapCollider : MonoBehaviour {
    
    /*
    public int force;
    private BoxCollider2D boundary; // The boundary is the collision collider that detects when the player is in range.

    private void OnCollisionEnter2D(Collision2D collision)
    {
        collision.gameObject.rigidbody.AddForce(Vector2.up * force);
    }
    */

}
